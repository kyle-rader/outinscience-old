'use strict';

angular.module('users').controller('PuzzleHuntPasswordController', ['$scope', '$stateParams', '$http', '$location', 'PuzzleAuth', 'Authentication', '$window',
  function($scope, $stateParams, $http, $location, PuzzleAuth, Authentication, $window) {
	$window.location.href = 'https://wwupuzzlehunt.com';
    };
  }
]);
